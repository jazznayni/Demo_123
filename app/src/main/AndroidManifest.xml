<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.arora.tusha.demo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SearchV">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- <activity android:name=".TextWatch"></activity> -->
        <!-- <activity android:name=".VerticalScroll"></activity> -->
        <!-- <activity android:name=".HorizontalScroll"></activity> -->
        <!-- <activity android:name=".ProgressB"></activity> -->
        <!--<activity android:name=".DigitalAnalogCclock" />-->
        <!-- <activity android:name=".TimePic"></activity> -->
        <!-- <activity android:name=".ImpliciitInten"></activity> -->
        <!-- <activity android:name=".ExplicitInten"></activity> -->
         <!--<activity android:name=".ExplicitInten2" />-->
        <!-- <activity android:name=".RecyclerV"></activity> -->
        <!-- <activity android:name=".ShareAppData"></activity> -->
        <!-- <activity android:name=".SplashScreen"></activity> -->
        <!-- <activity android:name=".Spinn"></activity> -->
        <!--<activity android:name=".Main2Activity"></activity>-->
    </application>

</manifest>